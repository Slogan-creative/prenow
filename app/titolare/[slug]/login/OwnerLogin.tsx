'use client';
import {useState} from 'react';
import {createBrowserSupabaseClient} from '@/lib/supabase/client';
export default function OwnerLogin({slug}:{slug:string}){const [busy,setBusy]=useState(false),[error,setError]=useState('');return <main className="owner-login"><div><div className="owner-login-brand">Prenow</div><h1>Gestisci la tua attività</h1><p>Accedi alla dashboard riservata al titolare.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);const db=createBrowserSupabaseClient();const r=await db.auth.signInWithPassword({email:String(f.get('email')),password:String(f.get('password'))});if(r.error){setError('Credenziali non valide.');setBusy(false);return}window.location.replace(`/titolare/${encodeURIComponent(slug)}`)}}><label>Email<input name="email" type="email" required autoComplete="username"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label>{error&&<p role="alert">{error}</p>}<button disabled={busy}>{busy?'Accesso…':'Accedi'}</button></form></div></main>}
